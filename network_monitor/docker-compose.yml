version: "3.9"
services:
  network-monitor:
    build: .
    container_name: network-monitor
    network_mode: host
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./docs:/app/docs
    command: ["python", "main.py", "--loop"]
